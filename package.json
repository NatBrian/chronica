{
  "name": "chronica",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node scripts/lint-sim.mjs && vitest run",
    "test:watch": "vitest",
    "lint:sim": "node scripts/lint-sim.mjs",
    "soak": "vitest run test/soak.test.ts"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
